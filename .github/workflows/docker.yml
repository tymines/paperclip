# ponytail: disabled — no Docker/GHCR env on tymines fork.
# Original workflow preserved in git history. Re-enable by reverting this commit.
name: Docker (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: exit 0
