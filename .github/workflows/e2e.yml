# ponytail: disabled — manual e2e only; no automated trigger needed on fork.
# Original workflow preserved in git history. Re-enable by reverting this commit.
name: E2E Tests (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: exit 0
