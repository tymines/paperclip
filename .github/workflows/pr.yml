# ponytail: disabled — fork targets integration, not master.
# The upstream PR workflow (typecheck/tests/e2e) only fires on PRs to master,
# which this fork doesn't use. Keep for reference; gate to avoid noise.
# Original workflow preserved in git history. Re-enable by reverting this commit.
name: PR (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: exit 0
