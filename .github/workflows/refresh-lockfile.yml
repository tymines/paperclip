# ponytail: disabled — fork doesn't need upstream lockfile refresh auto-merge loop.
# Original workflow preserved in git history. Re-enable by reverting this commit.
name: Refresh Lockfile (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: exit 0
