# ponytail: disabled — no Docker release smoke env on fork.
# Original workflow preserved in git history. Re-enable by reverting this commit.
name: Release Smoke (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: exit 0
