BRAIN BUILD — PROJECT STATE (ANCHOR)

Goal: Make Paperclip's FleetBrainView look like brain_concept.png — central energy core firing radiating filaments into 6 dense glowing lobes in a symmetric radial layout, live data only.

Target file (work ONLY here): augiais-mac-mini:/Users/augi/paperclip/ui/src/components/knowledge-graph/FleetBrainView.tsx. Live preview 127.0.0.1:5173 /knowledge-graph. DEAD-END, never deliver: C:\Users\Augi-T1\dev\fleet-brain-view*.html.

Executor: ARES, over SSH into Box 1 (access already works — Ares did the first edit this way). Hermes is NOT available (Box 1 gateway down). Zeus dispatches + reviews, doesn't edit directly.

Already done (don't redo): real component upgraded to ~970 lines, builds clean, backup *.bak.ares-neural-v3-*; HUD chrome works; Dreams panel on real GET /api/fleet-kb/dreams; 2 views only; 6 regions defined (CORE/HIPPOCAMPUS/BROCA/TEMPORAL/VISUAL/DREAMS).

The actual rebuild (NOT done): first pass only re-tinted the old force-graph + HUD. Build: (1) radial six-lobe layout around a core, NOT a force-graph blob; (2) central energy core firing radiating filaments into each lobe; (3) dense per-lobe particle glow (key miss); (4) glow stays toned down.

Data honesty (non-negotiable): ~91 nodes/403 synapses live — density is a rendering treatment; every cluster maps to a real record, every metric live. No fabricated entities.

Scoring: direct visual comparison to brain_concept.png, NOT a checklist. Not done until it reads like the image.

DoD: rebuilt real component, clean build, screenshot from :5173, honest score vs concept, openable live in Paperclip.

Parked (don't touch): Box 1 gateway crash (separate), Augi benched, memory migration paused.
