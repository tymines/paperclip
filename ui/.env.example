# Paperclip UI — example env
# World View tab: base URL of the host-portable worldview-collector service.
# Defaults to localhost (Box 1). Point at Box 2's tailnet host to offload, e.g.
#   VITE_WORLDVIEW_API_URL=http://augibot2.<tailnet>.ts.net:8788
VITE_WORLDVIEW_API_URL=http://localhost:8788
